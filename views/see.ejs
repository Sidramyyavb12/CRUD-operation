<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Post Details</title>
</head>
<body>
    <h1>Post Details</h1>
    <div class="containers">
        <div class="posts">
            <p>Post ID: <%= post.id %></p>
            <h2>Username: @<%= post.username %></h2>
            <p>Email: <%= post.email %></p>
            <p>Age: <%= post.age %></p>
            <p>Address: <%= post.address %></p>
            <p>Education: <%= post.education %></p>
            <p>Date of Birth: <%= post.dob %></p>
            <h4>Content: <%= post.content %></h4>
            </div>
            <% if (post.image) { %>
                <img src="<%= post.image %>" alt="Post Image" class="photo">
            <% } %>
       
    </div>
    
</body>
</html>
